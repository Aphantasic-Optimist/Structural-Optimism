# Central Configuration
# All URLs and repo references should derive from this file

# GitHub Repository
github:
  owner: Aphantasic-Optimist
  repo: Structural-Optimism
  branch: master

# Derived URLs (computed from above)
# site_url: https://{owner}.github.io/{repo}/
# repo_url: https://github.com/{owner}/{repo}

# To update: Change owner/repo above, then run:
#   python scripts/sync-config.py
